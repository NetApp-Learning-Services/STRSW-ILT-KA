apiVersion: v1
kind: Pod
metadata:
  name: webserver
spec:
  containers:
  - name: app
    image: httpd
    ports:
    - containerPort: 80
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"